<UserControl x:Class="askMeWindows.Views.HistoryControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="800">
    <Border Background="White" CornerRadius="8" Padding="15">
        <StackPanel>
            <TextBlock Style="{StaticResource LabelTextBlockStyle}" Text="検出履歴"/>
            <ScrollViewer VerticalScrollBarVisibility="Auto" Height="300">
                <ItemsControl ItemsSource="{Binding InstanceHistory}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#F9F9F9" BorderBrush="#E0E0E0" 
                                    BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,5">
                                <StackPanel>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock FontSize="12" FontWeight="Bold" Text="{Binding WorldName}"/>
                                        <TextBlock Grid.Column="1" FontSize="11" Foreground="#999999"
                                                   Text="{Binding DetectedAt, StringFormat='{}{0:HH:mm:ss}'}"/>
                                    </Grid>
                                    <TextBlock FontSize="10" Foreground="#666666" 
                                               Margin="0,5,0,0"
                                               TextTrimming="CharacterEllipsis" FontFamily="Consolas"
                                               Text="{Binding InstanceId}"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </StackPanel>
    </Border>
</UserControl>
